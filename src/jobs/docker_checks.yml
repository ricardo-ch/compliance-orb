description: >
  This job runs security checks on docker images
executor:
  name: default
  username: << parameters.executor_registry_username >>
  password: << parameters.executor_registry_password >>
parameters:
  executor_registry_username:
    type: string
  executor_registry_password:
    type: string
  image_name_file:
    type: string
    description: File where Docker image name is stored
  username:
    type: string
  password:
    type: string
  url:
    type: string
    default: ""
steps:
  - check_dockerroot:
      image_file: << parameters.image_name_file >>
      username: << parameters.username >>
      password: << parameters.password >>
      url: << parameters.url >>
